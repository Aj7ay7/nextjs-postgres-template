# PostgreSQL (used by docker-compose db service and to build DATABASE_URL)
POSTGRES_USER=myuser
POSTGRES_PASSWORD=changeme_use_strong_password
POSTGRES_DB=mydatabase

# Optional: for local runs or drizzle-kit (host machine → db)
# DATABASE_URL_EXTERNAL=postgres://myuser:changeme_use_strong_password@localhost:5432/mydatabase
